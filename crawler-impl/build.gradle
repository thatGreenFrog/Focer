dependencies {
    implementation project(":page-parser")
    implementation project(":db-connection")
}

jar{
    manifest{
        attributes(
            'Main-Class': 'lv.greenfrog.crawler.FocerCrawlTopology'
        )
    }
    baseName "focer"
    doLast {
        project.copy{
            from jar.archiveFile
            into "$rootDir/output"
        }
    }
}